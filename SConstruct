if 'tests' in COMMAND_LINE_TARGETS:
	SConscript('tests' + '/SConscript', variant_dir='build', src_dir='sources', duplicate=0)
else:
	SConscript('SConscript', variant_dir='build', src_dir='sources', duplicate=0)